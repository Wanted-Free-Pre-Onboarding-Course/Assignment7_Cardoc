export declare class TireInfoDto {
    name: string;
    width: number;
    aspectRatio: number;
    diameter: number;
    unit: string;
    multiValues: string;
    trimId: number;
    constructor(name: string, width: number, aspectRatio: number, diameter: number, unit: string, multiValues: string, trimId: number);
}
